name: purpleair-calib
channels:
  - conda-forge
  - defaults
dependencies:
  # Python version
  - python=3.9

  # Core scientific computing
  - numpy>=1.21.0
  - pandas>=1.3.0
  - scipy>=1.7.0

  # Machine learning
  - scikit-learn>=1.0.0
  - xgboost>=1.6.0
  - lightgbm>=3.3.0

  # Hyperparameter optimization
  - optuna>=3.0.0

  # Geospatial
  - rasterio>=1.2.0
  - geopandas>=0.10.0
  - pyproj>=3.2.0
  - shapely>=1.8.0

  # Climate data
  - netcdf4>=1.5.8
  - xarray>=0.20.0
  - cfgrib>=0.9.10

  # Visualization
  - matplotlib>=3.4.0
  - seaborn>=0.11.0
  - plotly>=5.3.0
  - folium>=0.12.0

  # Utilities
  - pyyaml>=5.4.0
  - python-dotenv>=0.19.0
  - tqdm>=4.62.0
  - joblib>=1.1.0
  - requests>=2.26.0
  - aiohttp>=3.8.0

  # Jupyter
  - jupyter>=1.0.0
  - jupyterlab>=3.0.0
  - ipywidgets>=7.6.0

  # Statistical analysis
  - statsmodels>=0.13.0

  # Testing and code quality
  - pytest>=6.2.0
  - pytest-cov>=3.0.0
  - black>=21.0
  - flake8>=3.9.0

  # Documentation
  - sphinx>=4.0.0
  - sphinx-rtd-theme>=1.0.0
  - nbsphinx>=0.8.0

  # Time utilities
  - pytz>=2021.3

  # Parallel processing
  - dask>=2021.10.0

  # File I/O
  - h5py>=3.6.0
  - pytables>=3.7.0

  # pip-only packages (installed via pip after conda environment)
  - pip>=21.0
  - pip:
    - catboost>=1.0.0
    - shap>=0.41.0
    - cdsapi>=0.5.0
    - loguru>=0.5.3
    - gradio>=3.0.0
